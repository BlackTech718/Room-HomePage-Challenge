document.addEventListener("click",showlist);

function showlist(){
    var nlist = document.getElementsByClassName(".nav").childNodes
    if (nlist = "flex")Then 
        nlist.style.display = "none"
}
